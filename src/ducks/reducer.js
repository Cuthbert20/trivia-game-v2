//INITIAL STATE
const initialState = {
  userId: null,
  email: "",
  username: "",
  profilePic: "",
  firstName: "",
  lastName: ""
};

//ACTION CONSTANTS

//ACTION BUILDERS

//REDUCER
export default (state = initialState, action) => {
  switch (action.type) {
    default:
      return state;
  }
};
